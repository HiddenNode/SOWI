{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "cmake --build ./bin",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}